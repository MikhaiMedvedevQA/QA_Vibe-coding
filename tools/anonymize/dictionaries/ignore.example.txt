# White-list проверенных пропусков ревью — по одной строке.
# Слова/фразы, которые человек разобрал и решил НЕ анонимизировать: повторные
# прогоны не флагают их как ревью-кандидатов. Регистр не важен.
#
# Сюда класть ТОЛЬКО проверенные осмысленные значения (не продукты/СК/банки —
# те добавлять в products.txt / insurance.txt / banks.txt). ignore.txt — для
# осмысленных значений, которые детектор по ошибке принимает за сущность.
# Это пример — реальный ignore.txt в репозиторий не попадает.

# Примеры (замените/дополните своими):
Пример-стоп-слово